<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.security.mapper.UserMapper">

    <resultMap id="PermissionResultMap" type="com.security.model.po.Permission">
        <id column="perm_id" property="id"/>
        <result column="permission_name" property="permissionName"/>
        <result column="permission_description" property="description"/>
    </resultMap>

    <resultMap id="RoleResultMap" type="com.security.model.po.Role">
        <id column="role_id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="role_description" property="description"/>
        <collection property="permissions" ofType="com.security.model.po.Permission"
                    resultMap="PermissionResultMap"/>
    </resultMap>

    <resultMap id="UserResultMap" type="com.security.model.po.User">
        <id column="user_id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="enabled" property="enabled"/>
        <collection property="roles" ofType="com.security.model.po.Role"
                    resultMap="RoleResultMap"/>
    </resultMap>

    <select id="selectUserWithRolesAndPermissions" resultMap="UserResultMap">
        SELECT u.id AS user_id,
               u.username,
               u.password,
               u.enabled,
               r.id AS role_id,
               r.role_name,
               r.description AS role_description,
               p.id AS perm_id,
               p.permission_name,
               p.description AS permission_description
        FROM users u
                 LEFT JOIN user_roles ur ON u.id = ur.user_id
                 LEFT JOIN roles r ON ur.role_id = r.id
                 LEFT JOIN role_permissions rp ON r.id = rp.role_id
                 LEFT JOIN permissions p ON rp.permission_id = p.id
        WHERE u.username = #{username}
    </select>

</mapper>
